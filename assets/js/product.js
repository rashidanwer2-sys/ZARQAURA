document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('[data-product]');if(!root)return;const id=new URLSearchParams(location.search).get('id');const p=PRODUCTS.find(x=>x.id===id&&x.active);if(!p){root.innerHTML='<div class="panel"><h2>Product not found</h2><a class="btn" href="shop.html">Back to Shop</a></div>';return}root.innerHTML=`<div class="product-layout"><div class="product-main-image"><div class="product-image">${ZARQAURA.imageMarkup(p)}</div></div><div class="product-info"><div class="eyebrow">${p.category}</div><h1>${p.name}</h1><div class="price" style="font-size:28px">${ZARQAURA.money(p.price)}</div><p>${p.description}</p><p><strong>SKU:</strong> ${p.id}</p><p><strong>Availability:</strong> ${p.stock>0?`${p.stock} in stock`:'Out of stock'}</p><div class="notice">${SITE_CONFIG.shippingText} • ${SITE_CONFIG.deliveryText}</div><div class="actions"><button class="btn" data-add="${p.id}" ${p.stock<=0?'disabled':''}>Add to Cart</button><a class="btn secondary" href="shop.html">Continue Shopping</a></div></div></div>`});
